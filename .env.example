# Copy these into Vercel → Project → Settings → Environment Variables

# Optional: verify webhook authenticity (recommended)
WEBHOOK_SECRET=

# Optional: enable autonomous issue creation
# Use a fine-grained PAT if possible; it should have access only to the repos you want LORD to act on.
GITHUB_TOKEN=
